diff --git a/leaderboard/leaderboard/leaderboard_evaluator.py b/leaderboard/leaderboard/leaderboard_evaluator.py
index 2f91b57..8df8114 100644
--- a/leaderboard/leaderboard/leaderboard_evaluator.py
+++ b/leaderboard/leaderboard/leaderboard_evaluator.py
@@ -199,12 +199,20 @@ class LeaderboardEvaluator(object):
         Prepares the simulation by getting the client, and setting up the world and traffic manager settings
         """
         self.carla_path = os.environ["CARLA_ROOT"]
-        args.port = find_free_port(args.port)
-        cmd1 = f"{os.path.join(self.carla_path, 'CarlaUE4.sh')} -RenderOffScreen -nosound -carla-rpc-port={args.port} -graphicsadapter={args.gpu_rank}"
-        self.server = subprocess.Popen(cmd1, shell=True, preexec_fn=os.setsid)
-        print(cmd1, self.server.returncode, flush=True)
-        atexit.register(os.killpg, self.server.pid, signal.SIGKILL)
-        time.sleep(30)
+        self.server = None
+        # PICDRIVE: attach to a server that is already running instead of
+        # spawning a headless one. find_free_port would otherwise step around
+        # the occupied port and start a second, -RenderOffScreen server, which
+        # is why a manually launched window never shows the evaluation.
+        if os.environ.get("PICDRIVE_EXTERNAL_CARLA"):
+            print(f"using external CARLA server on port {args.port}", flush=True)
+        else:
+            args.port = find_free_port(args.port)
+            cmd1 = f"{os.path.join(self.carla_path, 'CarlaUE4.sh')} -RenderOffScreen -nosound -carla-rpc-port={args.port} -graphicsadapter={args.gpu_rank}"
+            self.server = subprocess.Popen(cmd1, shell=True, preexec_fn=os.setsid)
+            print(cmd1, self.server.returncode, flush=True)
+            atexit.register(os.killpg, self.server.pid, signal.SIGKILL)
+            time.sleep(30)
             
         attempts = 0
         num_max_restarts = 20
@@ -497,7 +505,8 @@ class LeaderboardEvaluator(object):
             self.statistics_manager.compute_global_statistics()
             self.statistics_manager.validate_and_write_statistics(self.sensors_initialized, crashed)
         
-        if crashed:
+        # PICDRIVE: never kill a server this process did not start.
+        if crashed and not os.environ.get("PICDRIVE_EXTERNAL_CARLA"):
             cmd2 = "ps -ef | grep '-graphicsadapter="+ str(args.gpu_rank) + "' | grep -v grep | awk '{print $2}' | xargs -r kill -9"
             server = subprocess.Popen(cmd2, shell=True, preexec_fn=os.setsid)
             atexit.register(os.killpg, server.pid, signal.SIGKILL)
diff --git a/leaderboard/leaderboard/scenarios/scenario_manager.py b/leaderboard/leaderboard/scenarios/scenario_manager.py
index f71b475..0c75e69 100644
--- a/leaderboard/leaderboard/scenarios/scenario_manager.py
+++ b/leaderboard/leaderboard/scenarios/scenario_manager.py
@@ -11,6 +11,7 @@ It must not be modified and is for reference only!
 """
 
 from __future__ import print_function
+import os
 import signal
 import sys
 import time
@@ -227,9 +228,15 @@ class ScenarioManager(object):
             if self.scenario_tree.status != py_trees.common.Status.RUNNING:
                 self._running = False
 
-            ego_trans = self.ego_vehicles[0].get_transform()
-            self._spectator.set_transform(carla.Transform(ego_trans.location + carla.Location(z=70),
-                                                          carla.Rotation(pitch=-90)))
+            # PICDRIVE: this top-down view runs every tick, after the agent has
+            # already placed its chase camera in run_step, so with both active
+            # the window flips between the two once per frame.
+            # "" is the stock leaderboard behaviour; "off" leaves the camera
+            # free so it can be flown by hand in the window.
+            if os.environ.get("PICDRIVE_SPECTATOR", "") in ("", "bev"):
+                ego_trans = self.ego_vehicles[0].get_transform()
+                self._spectator.set_transform(carla.Transform(ego_trans.location + carla.Location(z=70),
+                                                              carla.Rotation(pitch=-90)))
 
     def get_running_status(self):
         """
diff --git a/leaderboard/leaderboard/utils/route_parser.py b/leaderboard/leaderboard/utils/route_parser.py
index c738d23..693a119 100644
--- a/leaderboard/leaderboard/utils/route_parser.py
+++ b/leaderboard/leaderboard/utils/route_parser.py
@@ -129,7 +129,7 @@ class RouteParser(object):
                 scenario_config.name = scenario.attrib.get('name')
                 scenario_config.type = scenario.attrib.get('type')
 
-                for elem in scenario.getchildren():
+                for elem in list(scenario):
                     if elem.tag == 'trigger_point':
                         scenario_config.trigger_points.append(convert_elem_to_transform(elem))
                     elif elem.tag == 'other_actor':
diff --git a/scenario_runner/srunner/tools/route_parser.py b/scenario_runner/srunner/tools/route_parser.py
index 89f1995..63fd30a 100644
--- a/scenario_runner/srunner/tools/route_parser.py
+++ b/scenario_runner/srunner/tools/route_parser.py
@@ -80,7 +80,7 @@ class RouteParser(object):
                 scenario_config.name = scenario.attrib.get('name')
                 scenario_config.type = scenario.attrib.get('type')
 
-                for elem in scenario.getchildren():
+                for elem in list(scenario):
                     if elem.tag == 'trigger_point':
                         scenario_config.trigger_points.append(convert_elem_to_transform(elem))
                     elif elem.tag == 'other_actor':
diff --git a/scenario_runner/srunner/tools/scenario_parser.py b/scenario_runner/srunner/tools/scenario_parser.py
index 0de98fa..d35e7ed 100644
--- a/scenario_runner/srunner/tools/scenario_parser.py
+++ b/scenario_runner/srunner/tools/scenario_parser.py
@@ -68,7 +68,7 @@ class ScenarioConfigurationParser(object):
                 config.name = scenario_config_name
                 config.type = scenario_config_type
 
-                for elem in scenario.getchildren():
+                for elem in list(scenario):
                     # Elements with special parsing
                     if elem.tag == 'ego_vehicle':
                         config.ego_vehicles.append(ActorConfigurationData.parse_from_node(elem, 'hero'))
